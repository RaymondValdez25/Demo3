name: Pipeline
on: [push]
jobs:
  run-unit-tests:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - run: |
          cd ServerSide
          node testAllQuotes.js
  
  run-EC2-instance:
    needs: run-unit-tests
    runs-on: ubuntu-latest
    steps:
      - run: aws --version
      
  deploy:
    name: Upload to Amazon S3
    runs-on: ubuntu-latest
    # These permissions are needed to interact with GitHub's OIDC Token endpoint.
    permissions:
      id-token: write
      contents: read
    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Configure AWS credentials from Test account
      uses: aws-actions/configure-aws-credentials@v1
      with:
        #aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
        #aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
        #aws-region: us-east-2
        #role-to-assume: ${{ secrets.AWS_ROLE_TO_ASSUME }}
        #role-external-id: ${{ secrets.AWS_ROLE_EXTERNAL_ID }}
        #role-duration-seconds: 1200
        #role-session-name: MySessionName
        aws-access-key-id: AKIAV3IW62MKEATQAYF3
        aws-secret-access-key: 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
        aws-region: us-east-2
    
    - run:
         aws --region us-east-2 ec2 run-instances \
       --dry-run \
       --image-id $AWS_LINUX_AMI --count 1 \
       --instance-type t2.micro \
       --subnet-id $MY_PUBLIC_SUBNET \
       --security-group-ids $MY_SECURITY_GROUP \
       --key-name $MY_KEY_PAIR

